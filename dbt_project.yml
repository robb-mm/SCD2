name: "robb_dbt_project"
version: "1.0.0"
config-version: 2

profile: "default"

vars:
   wrk_schema: BRONZE
   file_format_json: MYDB.BRONZE.MY_CSV_FORMAT
   purge_status: FALSE
   stage_name: MYDB.BRONZE.MY_S3_STAGE
   rawhist_db: MYDB
   
model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"

clean-targets:
  - "target"
  - "dbt_packages"

models:
  robb_dbt_project:
    silver:
      +schema: silver
      +materialized: table
    gold:
      +schema: gold
      +materialized: view
